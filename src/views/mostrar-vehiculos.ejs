<!DOCTYPE html>
<html lang="es">
  <%- include("partials/header")%>
  <body>
    <%- include("partials/nav")%>
    <div class="container p-4">
      <div class="row">
        <div class="card-columns">
          <% veh.forEach(function(moto){ %>
          <div class="card">
            <div class="card-header bg-success text-white text-center">
              <h2>Datos del vehiculo.</h2>
            </div>
            <div class="card-body">
              <h3 class="text-center text-success font-weight-bold">
                Foto de la placa.
              </h3>
              <div class="col-xl-12 mx-auto my-4">
                <img
                  width="100%" height="auto" src="<%= moto.url_img %>" alt="Foto de la placa"
                />
                <div class="form-group">
                  <form action="/registrar-salida" method="POST">
                    <input type="text" name="fotoPlaca" style="display: none;" value="<%= moto.url_img %>">
                     <!--<input type="date" name="fecha" required placeholder="fecha">-->
                     <input class="btn btn-success btn-block my-4"  type="submit" value="Registrar salida de este vehiculo">
                   </form>
                </div>
              </div>
            </div>
          </div>
          <% }); %>
        </div>
      </div>
       <div class="row">
        <div class="col-xl-4 mx-auto my-2">
            <div class="card">
              <div class="card-header bg-success">
                <p class="text-center text-white">
                  Si el conductor tiene un vehiculo diferente le puedes registrar
                  otro aquí.
                </p>
              </div>
              <div class="card-body">
                <form class="registrar" action="/registrar-otro" method="post">
                  <div class="form-group">
                    <input type="text" style="display: none;" name="conductor" value="<%= id %>" />
                  </div>
                  <div class="form-group">
                    <input
                      class="btn btn-success btn-block" type="submit" name="registrar-otro-vehiculo" value="Registrar otro vehiculo"
                    />
                  </div>
                </form>
              </div>
            </div>
          </div>
       </div>
    </div>
  </body>
</html>
